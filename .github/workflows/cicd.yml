name: GuestBook CI/CD Pipeline

on:
  push:

jobs:
  ci-job:
    runs-on: ubuntu-latest
    steps:
    - name: Check Out
      uses: actions/checkout@v3

    - name: Maven Build
      run: |
        ./mvnw clean package

    - name: Build 결과 확인
      run: |
        ls -l target/
